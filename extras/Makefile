.PHONY: all artist_to_albumartist clean

all artist_to_albumartist clean:
	@printf '%s\n' \
		'The extras subdirectory Makefile is unsupported during migration.' >&2
	@printf '%s\n' \
		'Run supported build targets from the project root Makefile.' >&2
	@exit 1
